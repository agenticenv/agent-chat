# Local dev: Backend URL for Vite proxy (npm run dev).
# Default: http://localhost:8080
API_PROXY_TARGET=http://localhost:8080

# Docker: Backend API base URL at runtime. Default: /api (same-origin).
API_BASE=/api
